{
  "name": "demo-claudecode",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev -w @invoice/api\" \"npm run dev -w @invoice/web\"",
    "dev:api": "npm run dev -w @invoice/api",
    "dev:web": "npm run dev -w @invoice/web",
    "build": "npm run build -w @invoice/api && npm run build -w @invoice/web",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
